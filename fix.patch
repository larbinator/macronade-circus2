diff --git a/src/components/LibraryPanel.tsx b/src/components/LibraryPanel.tsx
--- a/src/components/LibraryPanel.tsx
+++ b/src/components/LibraryPanel.tsx
@@ -53,1 +53,1 @@
-    loadManifest()
+    void loadManifest()
@@ -178,1 +178,1 @@
-        .catch(() => dispatchFallback())
+        .catch(() => { dispatchFallback(); })
@@ -229,1 +229,1 @@
-                  onClick={() => setSelectedLibraryAsset(item.path)}
+                  onClick={() => { setSelectedLibraryAsset(item.path); }}
@@ -230,1 +230,1 @@
-                  onDoubleClick={() => handleImport(category.id, item)}
+                  onDoubleClick={() => { handleImport(category.id, item); }}
diff --git a/src/components/ui/sidebar.tsx b/src/components/ui/sidebar.tsx
--- a/src/components/ui/sidebar.tsx
+++ b/src/components/ui/sidebar.tsx
@@ -91,1 +91,1 @@
-    return isMobile ? setOpenMobile((open) => !open) : setOpen((open) => !open)
+    isMobile ? setOpenMobile((open) => !open) : setOpen((open) => !open);
@@ -107,1 +107,1 @@
-    return () => window.removeEventListener("keydown", handleKeyDown)
+    return () => { window.removeEventListener("keydown", handleKeyDown); }
diff --git a/src/components/FloatingToolbar.tsx b/src/components/FloatingToolbar.tsx
--- a/src/components/FloatingToolbar.tsx
+++ b/src/components/FloatingToolbar.tsx
@@ -194,1 +194,1 @@
-    return () => observer.disconnect()
+    return () => { observer.disconnect(); }
@@ -203,1 +203,1 @@
-    return () => window.removeEventListener("resize", onResize)
+    return () => { window.removeEventListener("resize", onResize); }
@@ -337,1 +337,1 @@
-      onContextMenu={(event) => event.preventDefault()}
+      onContextMenu={(event) => { event.preventDefault(); }}
diff --git a/src/state/app-state.tsx b/src/state/app-state.tsx
--- a/src/state/app-state.tsx
+++ b/src/state/app-state.tsx
@@ -236,1 +236,1 @@
-          pantinId: item.attachment!.pantinId,
+          pantinId: item.attachment?.pantinId,
@@ -237,1 +237,1 @@
-          memberId: item.attachment!.memberId,
+          memberId: item.attachment?.memberId,
@@ -238,1 +238,1 @@
-          offsetX: lerp(item.attachment!.offsetX, other.attachment!.offsetX, t),
+          offsetX: lerp(item.attachment?.offsetX, other.attachment!.offsetX, t),
@@ -239,1 +239,1 @@
-          offsetY: lerp(item.attachment!.offsetY, other.attachment!.offsetY, t),
+          offsetY: lerp(item.attachment?.offsetY, other.attachment!.offsetY, t),
@@ -320,1 +320,1 @@
-    const snapshot = source.timeline.keyframeStates?.[frame]
+    const snapshot = source.timeline.keyframeStates[frame]
diff --git a/src/components/ui/scroll-area.tsx b/src/components/ui/scroll-area.tsx
--- a/src/components/ui/scroll-area.tsx
+++ b/src/components/ui/scroll-area.tsx
@@ -83,1 +83,1 @@
-    const observer = new ResizeObserver(() => scheduleUpdate())
+    const observer = new ResizeObserver(() => { scheduleUpdate(); })
@@ -86,1 +86,1 @@
-    return () => observer.disconnect()
+    return () => { observer.disconnect(); }
@@ -99,1 +99,1 @@
-    return () => viewport.removeEventListener("scroll", onScroll)
+    return () => { viewport.removeEventListener("scroll", onScroll); }
@@ -211,1 +211,1 @@
-      onPointerEnter={() => setIsHovering(true)}
+      onPointerEnter={() => { setIsHovering(true); }}
@@ -212,1 +212,1 @@
-      onPointerLeave={() => setIsHovering(false)}
+      onPointerLeave={() => { setIsHovering(false); }}
@@ -213,1 +213,1 @@
-      onWheel={() => setIsScrolling(true)}
+      onWheel={() => { setIsScrolling(true); }}
@@ -230,1 +230,1 @@
-            onPointerDown={(event) => startDrag(event, "y")}
+            onPointerDown={(event) => { startDrag(event, "y"); }}
@@ -246,1 +246,1 @@
-            onPointerDown={(event) => startDrag(event, "x")}
+            onPointerDown={(event) => { startDrag(event, "x"); }}
diff --git a/vite.config.ts b/vite.config.ts
--- a/vite.config.ts
+++ b/vite.config.ts
@@ -43,1 +43,1 @@
-      const id = attrs.id?.trim()
+      const id = attrs.id.trim()
@@ -161,1 +161,1 @@
-    configureServer(server: { watcher: any; ws: any }) {
+    configureServer(server: { watcher: unknown; ws: any }) {
diff --git a/src/components/OverlayToolbar.tsx b/src/components/OverlayToolbar.tsx
--- a/src/components/OverlayToolbar.tsx
+++ b/src/components/OverlayToolbar.tsx
@@ -56,1 +56,1 @@
-        onClick={() => setExpanded((prev) => !prev)}
+        onClick={() => { setExpanded((prev) => !prev); }}
@@ -98,1 +98,1 @@
-                  pressed={isControlled ? activeItems?.[item.id] : undefined}
+                  pressed={isControlled ? activeItems[item.id] : undefined}
diff --git a/src/components/SceneView.tsx b/src/components/SceneView.tsx
--- a/src/components/SceneView.tsx
+++ b/src/components/SceneView.tsx
@@ -262,1 +262,1 @@
-    return () => observer.disconnect()
+    return () => { observer.disconnect(); }
@@ -480,1 +480,1 @@
-    return () => cancelAnimationFrame(frame)
+    return () => { cancelAnimationFrame(frame); }
@@ -716,1 +716,1 @@
-                    onPointerDown={(event) => handlePointerDown(event, item.id)}
+                    onPointerDown={(event) => { handlePointerDown(event, item.id); }}
@@ -818,1 +818,1 @@
-                    startMemberRotation(event, handle.itemId, handle.memberId)
+                    { startMemberRotation(event, handle.itemId, handle.memberId); }
diff --git a/src/components/PropertiesPanel.tsx b/src/components/PropertiesPanel.tsx
--- a/src/components/PropertiesPanel.tsx
+++ b/src/components/PropertiesPanel.tsx
@@ -440,1 +440,1 @@
-                onChange={(event) => setAttachMember(event.target.value)}
+                onChange={(event) => { setAttachMember(event.target.value); }}
diff --git a/src/components/FloatingPanel.tsx b/src/components/FloatingPanel.tsx
--- a/src/components/FloatingPanel.tsx
+++ b/src/components/FloatingPanel.tsx
@@ -188,1 +188,1 @@
-    const onResize = () => clampToBounds()
+    const onResize = () => { clampToBounds(); }
@@ -190,1 +190,1 @@
-    return () => window.removeEventListener("resize", onResize)
+    return () => { window.removeEventListener("resize", onResize); }
@@ -279,1 +279,1 @@
-    return () => observer.disconnect()
+    return () => { observer.disconnect(); }
@@ -487,1 +487,1 @@
-      onContextMenu={(event) => event.preventDefault()}
+      onContextMenu={(event) => { event.preventDefault(); }}
@@ -497,1 +497,1 @@
-          onPointerMove: (event) => handleResizeMove(event.nativeEvent),
+          onPointerMove: (event) => { handleResizeMove(event.nativeEvent); },
@@ -498,1 +498,1 @@
-          onPointerUp: (event) => stopResize(event.nativeEvent),
+          onPointerUp: (event) => { stopResize(event.nativeEvent); },
@@ -499,1 +499,1 @@
-          onPointerCancel: (event) => stopResize(event.nativeEvent),
+          onPointerCancel: (event) => { stopResize(event.nativeEvent); },
diff --git a/src/components/team-switcher.tsx b/src/components/team-switcher.tsx
--- a/src/components/team-switcher.tsx
+++ b/src/components/team-switcher.tsx
@@ -69,1 +69,1 @@
-                onClick={() => setActiveTeam(team)}
+                onClick={() => { setActiveTeam(team); }}
